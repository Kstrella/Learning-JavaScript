// useful string properties & methods

let userName = "kevvv";
let phoneNumber = "123-456-7890";

//console.log(userName.length); //gives the length

//console.log(userName.charAt(0)); //char at a given index

//console.log(userName.indexOf("v")); // gives number of index

//console.log(userName.lastIndexOf("v")); // last occurance of the letter index

//userName = userName.trim(); //gets rid of empty spaces

//userName = userName.toUpperCase(); //makes upper case

//userName = userName.toLowerCase(); //makes lower case

phoneNumber = phoneNumber.replaceAll("-", ""); //replaces all of (a, with b)

console.log(phoneNumber);